#include <iostream>
#include <vector>
#include <utility>

using std::vector;
using std::pair;

class solution {
  public:
    int numOfArithSlices(const vector<int> & arr) {
        size_t begin = 0, end;
        
    }

  private:
    size_t findArithSubset(const vector<int> & arr, size_t start) {
        
    }

    bool isArith(const vector<int> & arr, size_t begin, size_t end) {
        if (end-begin < 2)
            return false;
        else {
            
        }
    }
};

int main() {
    vector<int> nums{1,2,3,4};
    
}
